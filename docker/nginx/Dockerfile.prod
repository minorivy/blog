FROM valian/docker-nginx-auto-ssl

ARG server_name

RUN addgroup -g 1000 -S www-data \
	&& adduser -u 1000 -D -S -G www-data www-data

ENTRYPOINT ["sed", "-i", "-e", 's/\${server_name}/'"${server_name}"'/g', "/etc/nginx/conf.d/default.conf"]